<?php foreach ($flash as $messages): ?>
<?php foreach ($messages as $message): ?>
<p><?= $message ?></p>
<?php endforeach; ?>
<?php endforeach; ?>
<div class="row">
  <div class="col">
    <form method="GET" action="users" class="d-flex justify-content-aroud">
      <input
        type="search"
        id="search"
        name="str"
        placeholder="Поиск по пользователям"
        value="<?= htmlspecialchars($str) ?>"
        class="form-control"
      />
      <button type="submit" class="btn btn-primary ms-3">Найти</button>
    </form>
  </div>
</div>

<div class="row mt-3">
  <div class="col">
    <a href="/users/new" class="btn btn-primary w-100">Создать нового пользователя</a>
  </div>
</div>
<div class="row">
  <div class="col">
    <?php if (!empty($users)): ?>
    <ul class="list-group mt-3">
      <?php foreach ($users as $user): ?>
      <li class="list-group-item d-flex justify-content-between">
        <a href="/users/<?= $user['id'] ?>" class="link-primary"
          ><?= htmlspecialchars($user['nickname']) ?></a
        >
      </li>
      <?php endforeach; ?>
    </ul>
    <?php endif; ?>
  </div>
</div>
